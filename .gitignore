# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Editor / OS
.vscode/
.idea/
.DS_Store

# PAmt recordings are data, never code
meetings/

# Clean-audio: the Clear SDK is installed into the package by `node setup.js`
# and then SHIPPED with the wheel/binary (see pyproject package-data).
# It stays out of git; setup.js reinstalls it locally for dev and the CI
# build step does it before PyInstaller.
pamt/node/node_modules/
