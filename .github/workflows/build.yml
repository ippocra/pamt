name: Build

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            suffix: linux
            pre-install: |
              sudo apt-get update
              sudo apt-get install -y portaudio19-dev
          - os: windows-latest
            suffix: windows
            pre-install: ""
          - os: macos-latest
            suffix: macos
            pre-install: |
              brew install portaudio

    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Platform pre-install
        shell: bash
        run: |
          ${{ matrix.pre-install }}

      - name: Install dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip
          pip install pyaudio pystray pillow pynput pyinstaller

      - name: Build binary (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          pyinstaller --onefile --clean --name pamt \
            --collect-all pamt \
            launcher.py
          mv dist/pamt "dist/pamt-${{ matrix.suffix }}"

      - name: Build binary (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          pyinstaller --onefile --clean --name pamt \
            --collect-all pamt \
            launcher.py
          mv dist/pamt.exe "dist/pamt-${{ matrix.suffix }}.exe"

      - name: Build binary (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          pyinstaller --onefile --clean --name pamt \
            --collect-all pamt \
            launcher.py
          mv dist/pamt "dist/pamt-${{ matrix.suffix }}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: pamt-${{ matrix.suffix }}
          path: dist/pamt-${{ matrix.suffix }}*

  # Create a GitHub Release when a version tag is pushed.
  release:
    needs: build
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # the tag points at the commit, not at main's HEAD
          ref: ${{ github.ref }}
          persist-credentials: false

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: releases

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=$(git describe --tags --exact-match)
          gh release create "$VERSION" \
            --title "PAmt $VERSION" \
            --notes "PAmt $VERSION — dual-track meeting recorder." \
            $(find releases -type f | sort)
